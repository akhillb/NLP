import sys 

f = open(sys.argv[1],"w") 
lines = f.readlines()
line_no = 1

user_sessions = {}

for line in lines:
    if line_no != 1:
	samayam = re.search("\d\d\d\d\-\d\d-\d\d \d\d\:\d\d\:\d\d",line)
	if samayam != None:
	    data = line.strip().split()
	    if not user_sessions.has_key(data[0]):
		user_sessions[data[0]]=[]
	    user_sessions.append((data[1],samayam))
    line_no = line_no + 1
